@model IEnumerable<Client.Model.Departl>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="pcoded-content">
    <div class="main-body">

        <div class="page-wrapper">
            <div cite="page-body">
                <div class="card">
                    <div class="card-header">
                        <h5>Department Info</h5>
                    </div>
                     <div class="my-2 px-3">


                        <p>
                            <a asp-action="Create" class="btn-outline-success p-2">Create New</a>
                        </p>
                     </div>

                    <div class="card-block">
                        <div class="table-responsive">
                         

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name_depart)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ID)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name_depart)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new {  id=item.ID }) |
                                                <a asp-action="Delete1" class="btn btn-danger" asp-route-id="@item.ID">Delete</a> 
                    
                @* @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ }) *@
            </td>
        </tr>
}
    </tbody>
</table>

                </div>
            </div>
        </div>
    </div>
</div>
    </div>
</div>